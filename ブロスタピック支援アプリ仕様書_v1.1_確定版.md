# ブロスタ ピック支援アプリ 仕様書 v1.1（確定版）

作成日: 2026-08-09
ステータス: 確定（残タスクは末尾「11. 残作業・実装時確認事項」参照）

v1.0からの変更点:

- マップ適性スコアは「勝率データからの自動生成 + 手動微調整」方式に確定（決定#1）
- 対象モードはランクマ全モードを最初からカバー（決定#2）
- 味方ピック入力とシナジー補正を正式仕様に昇格（決定#3）。スコア式に第4項を追加

---

## 1. 概要と目的

Brawl Stars（ブロスタ）において、**マップとキャラ相性に基づく最適ピックを提示する**個人用Webアプリを開発する。

想定する主な利用シーン:

- ランクマッチ（ドラフト形式）で、マップ・相手ピック・味方ピックを踏まえた自分のピック判断の補助
- マップローテーションを見て「このマップならどのキャラが強いか」を確認する Tier 表的な使い方

設計思想として、**相手・味方ピックの入力はいずれも任意**とする。何も入力しなければマップ別 Tier 表として機能し、入力するほどドラフト補助としての精度が上がる。1画面で全用途をカバーする。

対象モード: ランクマッチ対象の全モード（エメラルドハント、ブロストライカー、ホットゾーン、賞金稼ぎ、強奪、ノックアウト）。データが自動生成ベースのため、初期リリースから全モード対応する。

---

## 2. スコアリング仕様（コアロジック）

### 2.1 基本式

各キャラの推奨度は以下の合成スコアで算出する。

```
最終スコア = マップ適性スコア + 対敵相性補正 + 味方シナジー補正 + メタ係数
```

| 項目             | 変動性                 | データ源                              | 値域(目安)                             |
| ---------------- | ---------------------- | ------------------------------------- | -------------------------------------- |
| マップ適性スコア | パッチで緩やかに変動   | マップ別勝率から自動生成 + 手動上書き | 0〜10（中立=5）                        |
| 対敵相性補正     | ほぼ不変（構造的相性） | 手動定義（本アプリの独自価値）        | 相手1体につき -3〜+3                   |
| 味方シナジー補正 | ほぼ不変               | 手動定義                              | 味方1体につき -2〜+2、構成ルール加減点 |
| メタ係数         | パッチごとに変動       | 全体勝率から自動生成                  | -3〜+3                                 |

注: マップ適性を勝率から自動生成する場合、マップ別勝率には全体的な強さ（メタ）も混入する。二重計上を避けるため、マップ適性の自動生成では**「そのキャラの全体勝率との差分」**を使う（2.2参照）。メタ係数が「今どれだけ強いか」を、マップ適性が「このマップで平均よりどれだけ強いか」を受け持つ。

### 2.2 マップ適性スコアの自動生成 + 手動微調整

```
自動値 = clamp(5 + (マップ別勝率% - キャラ全体勝率%) × 0.5, 0, 10)
有効値 = 手動上書き値があればそれを優先、なければ自動値
信頼度: マップ別試合数が閾値M未満の場合、自動値を5(中立)方向に縮める
        自動値' = 5 + (自動値 - 5) × min(1, 試合数 / M)
```

- 手動上書きは `map_overrides.json` に理由付きで記録し、自動更新で消えないよう分離管理する。
- 上書き値と自動値が大きく乖離した場合（差3以上）、データ管理画面で警告表示し見直しを促す。

### 2.3 対敵相性補正の詳細

**2段構え**で定義する。

1. **ロール間相性（デフォルト値）**: 細分ロール × 細分ロール のマトリクス（約12×12 = 144マス）。例:「突進タンク vs 近距離火力 = -2」
2. **個別ペア上書き**: 特筆すべきキャラペアのみ個別定義し、ロール間相性より優先する。例:「エドガー vs シェリー = -3」

相手チームが複数入力されている場合、各相手との相性値を**合算**する。

### 2.4 味方シナジー補正の詳細

味方ピックが入力された場合に適用する。2つの要素の和とする。

**(a) ロール間シナジー**: 細分ロール × 細分ロール の味方版マトリクス。対敵相性と同じ2段構え（ロールデフォルト + 個別ペア上書き）。例:

- 「突進タンク × 回復サポート = +2」（前線維持が成立する）
- 「壁裏投げ × 壁裏投げ = -1」（火力が偏る）

**(b) 構成バランスルール**: チーム3体の構成全体を見た加減点。ルールベースで定義する。例:

- コントロール系モード（エメハン、ホットゾーン）で長射程・制圧系が0体 → 候補が該当ロールなら +1
- アサシン2体目 → -1（役割重複）
- 全員近距離 → 近距離候補に -1

ルールは `comp_rules.json` に宣言的に記述し、コード変更なしで追加・調整できるようにする（3.2参照）。

### 2.5 メタ係数の自動生成

全体勝率データから以下の変換で生成する。

```
メタ係数 = clamp((全体勝率% - 50) × 0.5, -3, +3) × 信頼度重み
信頼度重み = min(1, 試合数 / 閾値N)   // Nは例えば1000。要調整
```

- 変換パラメータ（傾き、clamp幅、閾値N/M）はすべて `config.json` で調整可能にする。
- 自動生成値は手動上書き可能とし、上書き時は理由を `note` に記録する。

### 2.6 スコア理由の提示

ランキング表示時、各キャラに**スコア内訳に基づく短い理由**を最大2つ添える（絶対値の大きい要素から優先）。例:

- 「このマップで平均+4%勝率（適性 8/10）」
- 「相手の◯◯に有利（+2）」
- 「味方の◯◯と好相性（+2）」
- 「今環境で好調（メタ +2）」

---

## 3. データモデル

データは「変わりにくい層（手作り・資産）」と「変わりやすい層（自動生成）」に分離する。

### 3.1 ファイル構成（すべてJSON）

| ファイル              | 内容                              | 更新頻度       | 更新方法                  |
| --------------------- | --------------------------------- | -------------- | ------------------------- |
| `brawlers.json`       | キャラマスタ + 細分ロール割り当て | 新キャラ追加時 | 自動取得 + ロールのみ手動 |
| `roles.json`          | 細分ロール定義                    | ほぼ不変       | 手動                      |
| `role_matchups.json`  | 対敵ロール相性マトリクス          | ほぼ不変       | 手動                      |
| `role_synergy.json`   | 味方ロールシナジーマトリクス      | ほぼ不変       | 手動                      |
| `pair_overrides.json` | 個別ペア上書き（対敵・味方両方）  | 随時追加       | 手動                      |
| `comp_rules.json`     | 構成バランスルール                | 随時追加       | 手動                      |
| `map_scores.json`     | マップ適性の自動生成値            | パッチごと     | 自動生成                  |
| `map_overrides.json`  | マップ適性の手動上書き            | 随時           | 手動                      |
| `meta.json`           | メタ係数                          | パッチごと     | 自動生成（手動上書き可）  |
| `config.json`         | 変換パラメータ、モード一覧等      | 随時           | 手動                      |

自動生成ファイル（map_scores, meta）と手動ファイルは**完全に分離**し、`update-data` 実行が手作業の資産を上書きしない構造を保証する。

### 3.2 スキーマ定義

**brawlers.json**

```json
{
  "updatedAt": "2026-08-09T00:00:00Z",
  "brawlers": [
    {
      "id": 16000000,
      "name": "シェリー",
      "nameEn": "Shelly",
      "officialClass": "Damage Dealer",
      "role": "close_dps",
      "imageUrl": "https://cdn.brawlify.com/brawlers/borderless/16000000.png"
    }
  ]
}
```

- `id`, `nameEn`, `officialClass`, `imageUrl` は BrawlAPI から自動取得。
- `role` のみ手動割り当て。未割り当てキャラはUI警告 + 公式クラスからの暫定推定で動作。

**roles.json** — 細分ロール定義（たたき台。割り当て作業時に確定）

```json
{
  "roles": [
    { "id": "dive_tank", "name": "突進タンク", "example": "ブル、ダリル" },
    { "id": "anchor_tank", "name": "居座りタンク", "example": "フランケン" },
    { "id": "assassin", "name": "アサシン", "example": "エドガー、レオン" },
    { "id": "mid_dps", "name": "中射程アタッカー", "example": "" },
    { "id": "close_dps", "name": "近距離火力", "example": "シェリー" },
    { "id": "sniper", "name": "長射程スナイパー", "example": "" },
    {
      "id": "thrower",
      "name": "壁裏投げ",
      "example": "バーリー、ダイナマイク"
    },
    { "id": "trap", "name": "設置・トラップ系", "example": "タラ、グロム" },
    { "id": "heal_support", "name": "回復サポート", "example": "" },
    { "id": "buff_support", "name": "バフサポート", "example": "" },
    { "id": "area_control", "name": "範囲制圧", "example": "ペニー、ジェシー" }
  ]
}
```

**role_matchups.json（対敵）**

```json
{
  "matchups": {
    "dive_tank": { "close_dps": -2, "thrower": 1, "sniper": 2 },
    "thrower": { "sniper": -2, "anchor_tank": 2 }
  }
}
```

- `matchups[A][B]` = 「ロールAのキャラが、相手にロールBがいるときに受ける補正」。
- 未定義ペアは 0。対称性は仮定しない。

**role_synergy.json（味方）**

```json
{
  "synergy": {
    "dive_tank": { "heal_support": 2, "dive_tank": -1 },
    "thrower": { "thrower": -1, "anchor_tank": 1 }
  }
}
```

- `synergy[A][B]` = 「ロールAのキャラが、味方にロールBがいるときに受ける補正」。
- こちらは実質対称にすることが多いが、形式上は非対称も許容する。

**pair_overrides.json**

```json
{
  "vsEnemy": [
    {
      "self": 16000015,
      "enemy": 16000000,
      "value": -3,
      "note": "エドガー vs シェリー: 接近しても迎撃される"
    }
  ],
  "withAlly": [
    {
      "self": 16000010,
      "ally": 16000020,
      "value": 2,
      "note": "コンボ成立の定番ペア"
    }
  ]
}
```

- `note` 必須。調整理由を残す。

**comp_rules.json（構成バランスルール）**

```json
{
  "rules": [
    {
      "id": "need_range_control",
      "when": {
        "modes": ["gemGrab", "hotZone"],
        "teamLacksRoles": ["sniper", "area_control", "mid_dps"]
      },
      "then": {
        "bonusToRoles": ["sniper", "area_control", "mid_dps"],
        "value": 1
      },
      "note": "コントロール系モードで射程・制圧不在なら該当ロール候補を加点"
    },
    {
      "id": "no_double_assassin",
      "when": { "teamHasRoleCount": { "assassin": 1 } },
      "then": { "bonusToRoles": ["assassin"], "value": -1 },
      "note": "アサシン2体目は役割重複"
    }
  ]
}
```

- `when` 条件: `modes`（対象モード限定、省略時全モード）、`teamLacksRoles`（味方にどのロールもいない）、`teamHasRoleCount`（指定ロールが既にn体以上）。
- `then`: 該当ロールの候補キャラへの加減点。
- 評価対象の「チーム」= 入力済み味方 + 評価中の候補キャラ自身。

**map_scores.json（自動生成）**

```json
{
  "updatedAt": "2026-08-09T00:00:00Z",
  "source": "brawltime",
  "maps": {
    "15000026": {
      "name": "Hard Rock Mine",
      "mode": "gemGrab",
      "scores": {
        "16000000": {
          "value": 6.2,
          "mapWinRate": 52.4,
          "globalWinRate": 50.1,
          "battles": 8400
        }
      }
    }
  }
}
```

- `scores` に載っていないキャラは中立値 5 とみなす。

**map_overrides.json（手動上書き）**

```json
{
  "overrides": [
    {
      "mapId": "15000026",
      "brawlerId": 16000000,
      "value": 8,
      "note": "統計は低いが自分の立ち回りだと強い"
    }
  ]
}
```

**meta.json（自動生成 + 手動上書き可）**

```json
{
  "updatedAt": "2026-08-09T00:00:00Z",
  "patchVersion": "任意入力",
  "coefficients": {
    "16000000": {
      "value": 1.5,
      "winRate": 53.1,
      "battles": 45200,
      "manualValue": null,
      "note": ""
    }
  }
}
```

- `manualValue` が非nullならそれを優先。自動更新は `value` のみ書き換え、`manualValue`/`note` は保持する。

### 3.3 相性・シナジー解決の優先順位

候補キャラ candidate に対して:

対敵（相手 enemy ごと）:

1. `pair_overrides.vsEnemy` に (candidate, enemy) があればその値
2. なければ `role_matchups[candidateのrole][enemyのrole]`
3. 未定義なら 0

味方（味方 ally ごと）:

1. `pair_overrides.withAlly` に (candidate, ally) があればその値
2. なければ `role_synergy[candidateのrole][allyのrole]`
3. 未定義なら 0

構成ルール: 上記とは独立に `comp_rules.json` を全ルール評価して合算。

---

## 4. データソースと更新パイプライン

### 4.1 マスタデータ: BrawlAPI

- ベースURL: `https://api.brawlapi.com`
- 認証不要・レート制限なし・CORS対応（`Access-Control-Allow-Origin: *`）。ブラウザから直接fetch可能。
- 使用エンドポイント:
  - `GET /v1/brawlers` — 全キャラ一覧（id, 名前, 公式クラス, 画像URL）
  - `GET /v1/maps` — 全マップ一覧（id, 名前, モード, マップ画像URL）
  - `GET /v1/gamemodes` — モード一覧
- 注意: BrawlAPIのマップ詳細 `stats[]` は現在常に空。勝率データはここからは取得できない。

### 4.2 勝率データ: Brawl Time Ninja（実装時に要検証）

- ランクマのマップ別キャラ勝率を公開。オープンソースで内部に集計データAPI（cube API）を持つ。
- 必要なデータ: (a) キャラ全体勝率 + 試合数、(b) マップ別キャラ勝率 + 試合数（ランクマ対象全モード分）
- **実装時に確認すること**: APIエンドポイント仕様と安定性、利用規約、アクセス頻度のマナー（低頻度・キャッシュ前提で設計）
- フォールバック: 取得不能時は `map_scores.json` / `meta.json` を手動編集に切り替え可能。スコア計算はデータ源に依存しない設計とする。

### 4.3 更新スクリプト `update-data`

Node.js の単体スクリプト。処理内容:

1. BrawlAPI からキャラ・マップのマスタを取得し `brawlers.json` を更新（`role` フィールドは保持）
2. 新キャラ（ロール未割り当て）を検出しコンソール警告
3. 勝率データを取得し、2.2 / 2.5 の変換式で `map_scores.json` と `meta.json` を生成（手動上書きファイル・フィールドは不変）
4. 各ファイルに `updatedAt` を記録
5. 取得した生の勝率データをローカルにキャッシュ（再実行時のAPI負荷軽減）

実行頻度: 手動実行を基本（パッチ後）。将来的に GitHub Actions 等での定期実行も可能な構成にする。

---

## 5. 画面仕様

### 5.1 メイン画面（1画面構成）

上から順に:

1. **モード・マップ選択部**
   - モードタブ（ランクマ全6モード）→ マップ選択（マップ画像サムネイル付き）
2. **ドラフト入力部（任意）**
   - 「相手」枠 最大3体 / 「味方」枠 最大2体 をキャラアイコン一覧からタップで選択
   - 検索・ロール絞り込み付き。選択済みは再タップで解除。BAN済みキャラの除外指定も可能（候補から非表示にするだけの軽い機能）
3. **おすすめランキング表示部**
   - 最終スコア降順に表示（上位15体程度 + 「すべて表示」）
   - 各行: キャラアイコン / 名前 / スコア / 理由（最大2つ、2.6参照）
   - タップでスコア内訳（適性・対敵・シナジー・メタ）を展開表示
   - 入力変更（相手・味方の追加/削除）で即時再計算

### 5.2 データ管理画面（開発者向けサブ画面）

- メタ係数・マップ適性の一覧と手動上書きUI（noteの入力必須）
- 自動値と上書き値の乖離警告（差3以上）
- ロール未割り当てキャラの警告と割り当てUI
- データ最終更新日時の表示

### 5.3 UI要件

- スマホブラウザ第一のレスポンシブ設計（試合の合間に片手で操作できること）
- キャラ画像・マップ画像は BrawlAPI の CDN URL を直接参照
- 日本語UI

---

## 6. 技術構成

| 項目           | 選定                                                    | 理由                                         |
| -------------- | ------------------------------------------------------- | -------------------------------------------- |
| 形態           | 静的Webアプリ（HTML/CSS/JS）                            | サーバー不要、スマホ利用可、ホスティング容易 |
| フレームワーク | 軽量構成（Vanilla JS または任意の小規模構成）           | 個人用途、依存最小                           |
| データ         | 静的JSON同梱 + BrawlAPI直接fetch                        | 4章参照                                      |
| 更新スクリプト | Node.js 単体スクリプト                                  | 4.3参照                                      |
| ホスティング   | GitHub Pages 等                                         | 無料・git pushで更新完結                     |
| 状態管理       | メモリ上 + localStorage（設定・ドラフト入力の一時保持） | 軽量アプリのため                             |

---

## 7. 開発フェーズ

### フェーズ1: 全モードTier表（MVP）

- BrawlAPIからキャラ・マップ取得、全キャラへの細分ロール割り当て
- `update-data` の骨格（マスタ取得部分）
- マップ適性（暫定は手動値または仮の自動値）+ メタ係数によるランキング表示
- ランクマ全6モードのマップに対応

### フェーズ2: ドラフト補助

- 相手・味方ピック入力UI、BAN除外
- 対敵相性マトリクス + 味方シナジーマトリクス + 構成ルールの定義と実装
- スコア理由・内訳表示

### フェーズ3: 自動更新の完成

- Brawl Time Ninja からの勝率取得を実装し、マップ適性・メタ係数の自動生成を完成
- 手動上書きレイヤー（map_overrides、manualValue）とデータ管理画面
- 個別ペア上書きの拡充

### フェーズ4以降（構想）

- 手持ちキャラ・パワーレベル考慮
- BAN候補の提案（相手視点で最高スコアのキャラ = BAN価値が高い、のロジック流用）
- 勝率データ取得の定期自動実行

---

## 8. 非機能要件

- **オフライン耐性**: 同梱JSONを既定とし、外部fetchは起動時の更新チェックに留める（API障害時も動作）
- **外部APIへの配慮**: 勝率取得は低頻度・手動実行、結果をローカルキャッシュ
- **保守性**: スコア計算はデータ源から独立。変換パラメータは `config.json` に集約。自動生成データと手動データの分離を厳守
- **応答性**: ピック入力ごとの再計算は全キャラ×相手3×味方2程度の計算のため、クライアント内で即時（<50ms）に完了する想定

---

## 9. 将来の変更に対する運用まとめ

| 変化イベント                     | 必要な作業                                               |
| -------------------------------- | -------------------------------------------------------- |
| バランス調整（バフ/ナーフ）      | `update-data` 実行のみ（マップ適性・メタ係数が自動追従） |
| 新キャラ追加                     | `update-data` 実行 + 細分ロールを1つ手動割り当て         |
| 新マップ追加                     | `update-data` 実行のみ（勝率が貯まるまでは中立値で表示） |
| 統計と自分の感覚のズレ           | `map_overrides.json` / `manualValue` に理由付きで上書き  |
| メタの構造変化（新ロール誕生等） | `roles.json` と各マトリクスに行列追加（稀）              |

---

## 10. 確定済みの設計判断（経緯の記録）

1. マップ適性: 勝率からの自動生成をベースに手動微調整レイヤーを重ねる。自動値は「マップ別勝率と全体勝率の差分」を使い、メタ係数との二重計上を回避
2. 対象モード: ランクマ全6モードを初期リリースからカバー（データ自動生成のため追加コストが小さい）
3. ドラフト入力: 相手だけでなく味方も入力可能とし、ロールシナジー + 構成バランスルールで補正
4. 細分ロール: 公式クラスでなく独自の11分類前後を使用（相性表の精度優先）
5. データ3層分離: 構造的な層（相性・シナジー）は手作り資産、変動する層（勝率由来）は自動生成、両者を物理的にファイル分離

---

## 11. 残作業・実装時確認事項

1. **細分ロールの最終確定**: 全キャラ（107体）を11分類に割り当てる作業。割り当て中に分類の過不足を見直す（フェーズ1で実施）
2. **Brawl Time Ninja のAPI検証**: エンドポイント仕様・利用規約・取得可能なデータ粒度（モード別/マップ別/ランクマ限定か）の確認（フェーズ3着手時）
3. **変換パラメータの調整**: 傾き0.5、閾値N/M等は実データを見て調整（フェーズ3）
4. **構成ルールの初期セット**: comp_rules の具体ルールをモードごとに数個ずつ定義（フェーズ2）
5. **相性・シナジーマトリクスの初期値入力**: 11×11×2枚 + 個別ペア。フェーズ2の主作業
